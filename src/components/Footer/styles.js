import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles(theme => ({
    footer: {
    },
    footerHomePage: {
        backgroundColor: '#000'
    },
    footerTitle: {
        fontWeight: '900 !important'
    }
}));